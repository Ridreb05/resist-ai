# File: frontend/.env.example
# Copy to .env.local

# API base URL — leave empty in Docker (nginx proxies /api)
VITE_API_URL=/api/v1

# Optional: set to your deployed backend URL during standalone dev
# VITE_API_URL=http://localhost:8000/api/v1
